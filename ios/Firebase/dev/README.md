# Development Firebase iOS configuration

Download `GoogleService-Info.plist` from the iOS app registered in the
`ota-management-platform` Firebase project and place it in this directory.
The bundle identifier in that file must match the development Xcode
configuration.
