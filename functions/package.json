{
  "name": "ota-cheshire-functions",
  "private": true,
  "main": "src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "eslint src test",
    "test": "node --test --test-force-exit test/*.test.js",
    "test:emulator": "node --test --test-force-exit test/*.emulator.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "eslint": "^9.39.0",
    "firebase": "^12.16.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
